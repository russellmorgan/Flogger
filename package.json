{
    "name": "flogger",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "build:pages": "vite build",
        "preview": "vite preview",
        "deploy:pages": "npm run build:pages && gh-pages -d dist"
    },
    "dependencies": {
        "phaser": "^3.90.0"
    },
    "devDependencies": {
        "gh-pages": "^6.1.1",
        "vite": "^7.1.3"
    }
}
